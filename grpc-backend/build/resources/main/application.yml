spring:
    application:
        name: grpc-backend

server:
    port: 19091

grpc:
    server:
        port: 29090

management:
    endpoints:
        web:
            exposure:
                include: health,info,prometheus
    endpoint:
        health:
            show-details: always