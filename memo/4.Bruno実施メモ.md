## Bruno 確認の前提

- bootRun は実施済み
- Bruno は未導入のため、現時点では Bruno 実行ベースの確認結果は未取得
- 先に Bruno を導入し、BFF に対して以下 2 API を固定 request で確認する
  - `GET /api/users/{id}`
  - `POST /api/orders`

確認方針:

- Bruno の接続先は `http://localhost:19090` に固定する
- 比較時は Bruno 側 request を変えず、BFF の `app.call-mode` のみ切り替える
- 確認観点は以下とする
  - ステータスコード
  - レスポンスボディ
  - エラー時の見え方
  - 手動デバッグのしやすさ
  - 利用者視点で差が出るか

## Bruno導入

[Bruno](https://www.usebruno.com/)からダウンロードして展開

### GET /api/users/{id} Bruno 確認メモ

- 実行手段:
  - Bruno
- 対象 request:
  - `GET users`
- 接続先:
  - `http://localhost:19090/api/users/1`
- 確認項目:
  - ステータスコード
  - レスポンスボディ
  - REST / gRPC 切替時の差分
  - 手動デバッグのしやすさ
- 実施結果:
  - Bruno から送信し、レスポンス確認まで実施済み
- 未整理:
  - REST / gRPC の並列比較結果
  - エラー時の見え方

### POST /api/orders Bruno 確認メモ

- 実行手段:
  - Bruno
- 対象 request:
  - `POST order`
- 接続先:
  - `http://localhost:19090/api/orders`
- リクエストボディ:
  - `{"userId":"1","itemCode":"ITEM-001","quantity":1}`
- 確認項目:
  - ステータスコード
  - レスポンスボディ
  - REST / gRPC 切替時の差分
  - 手動デバッグのしやすさ
  - エラー時の見え方
- 実施結果:
  - Bruno から送信し、レスポンス確認まで実施済み
- 未整理:
  - REST / gRPC の並列比較結果
  - バリデーションエラー時の見え方
