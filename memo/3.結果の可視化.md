## 1. Otel, jaegerの導入

### プロジェクトルートで実行

```shell
New-Item -ItemType Directory -Path .otel -Force | Out-Null

Invoke-WebRequest `
    -Uri "https://github.com/open-telemetry/opentelemetry-java-instrumentation/releases/latest/download/opentelemetry-javaagent.jar" `
    -OutFile ".otel/opentelemetry-javaagent.jar"
```

### dockerを起動

```shell
cd observability
docker compose up -d jaeger
```

### bffを起動する場合

BFF は 内部呼び出しの切替役 なので、app.call-mode=rest/grpcを付ける。


```shell
cd bff

$repoRoot = (Resolve-Path ..).Path

Remove-Item Env:JAVA_TOOL_OPTIONS -ErrorAction SilentlyContinue

$env:JAVA_TOOL_OPTIONS = "-javaagent:$repoRoot\.otel\opentelemetry-javaagent.jar"
$env:OTEL_SERVICE_NAME = "bff"
$env:OTEL_PROPAGATORS = "tracecontext,baggage"
$env:OTEL_TRACES_EXPORTER = "otlp"
$env:OTEL_METRICS_EXPORTER = "none"
$env:OTEL_LOGS_EXPORTER = "none"
$env:OTEL_EXPORTER_OTLP_PROTOCOL = "http/protobuf"
$env:OTEL_EXPORTER_OTLP_ENDPOINT = "http://localhost:4318"
.\gradlew.bat bootRun --args="--app.call-mode=rest"
```
### rest-backendを起動する場合

```shell
cd rest-backend

$repoRoot = (Resolve-Path ..).Path

Remove-Item Env:JAVA_TOOL_OPTIONS -ErrorAction SilentlyContinue

$env:JAVA_TOOL_OPTIONS = "-javaagent:$repoRoot\.otel\opentelemetry-javaagent.jar"
$env:OTEL_SERVICE_NAME = "rest-backend"
$env:OTEL_PROPAGATORS = "tracecontext,baggage"
$env:OTEL_TRACES_EXPORTER = "otlp"
$env:OTEL_METRICS_EXPORTER = "none"
$env:OTEL_LOGS_EXPORTER = "none"
$env:OTEL_EXPORTER_OTLP_PROTOCOL = "http/protobuf"
$env:OTEL_EXPORTER_OTLP_ENDPOINT = "http://localhost:4318"

.\gradlew.bat bootRun
```

### grpc-backendを起動する場合

```shell
cd grpc-backend

$repoRoot = (Resolve-Path ..).Path

Remove-Item Env:JAVA_TOOL_OPTIONS -ErrorAction SilentlyContinue

$env:JAVA_TOOL_OPTIONS = "-javaagent:$repoRoot\.otel\opentelemetry-javaagent.jar"
$env:OTEL_SERVICE_NAME = "grpc-backend"
$env:OTEL_PROPAGATORS = "tracecontext,baggage"
$env:OTEL_TRACES_EXPORTER = "otlp"
$env:OTEL_METRICS_EXPORTER = "none"
$env:OTEL_LOGS_EXPORTER = "none"
$env:OTEL_EXPORTER_OTLP_PROTOCOL = "http/protobuf"
$env:OTEL_EXPORTER_OTLP_ENDPOINT = "http://localhost:4318"

.\gradlew.bat bootRun
```

### 2. 動作確認

grpc/restそれぞれでAPIを10回ずつ叩いてみる。

```shell
Invoke-RestMethod http://localhost:19090/api/users/1
```

ブラウザで[URLを開く](http://localhost:16686)。
Serviceからbffを選択し、Traceが表示されているか確認。

